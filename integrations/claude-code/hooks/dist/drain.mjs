// mubit-memory runtime floor guard — generated by esbuild. Do not edit.
import { writeSync } from "fs";
var major = parseInt(String(process.versions.node).split(".")[0], 10);
if (!(major >= 20)) {
  try {
    writeSync(2, "mubit-memory requires Node >= 20 (package.json engines); found v" + process.versions.node + ". hook drain is disabled for this session.\n");
    writeSync(1, '{"suppressOutput":true}');
  } catch (e) {
  }
  process.exit(0);
}
globalThis.__mubitLauncherEntry = "./impl/drain.mjs";
import("./impl/drain.mjs").catch(function(err) {
  try {
    writeSync(2, "mubit-memory: hook drain failed to load: " + (err && err.message ? err.message : String(err)) + "\n");
    writeSync(1, '{"suppressOutput":true}');
  } catch (e) {
  }
  process.exit(0);
});
