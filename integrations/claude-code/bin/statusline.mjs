// mubit-memory runtime floor guard — generated by esbuild. Do not edit.
import { writeSync } from "fs";
var major = parseInt(String(process.versions.node).split(".")[0], 10);
if (!(major >= 20)) {
  try {
    writeSync(2, "mubit-memory requires Node >= 20 (package.json engines); found v" + process.versions.node + ". the status line is disabled for this session.\n");
  } catch (e) {
  }
  process.exit(0);
}
globalThis.__mubitLauncherEntry = "./impl/statusline.mjs";
import("./impl/statusline.mjs").catch(function(err) {
  try {
    writeSync(2, "mubit-memory: the status line failed to load: " + (err && err.message ? err.message : String(err)) + "\n");
  } catch (e) {
  }
  process.exit(0);
});
